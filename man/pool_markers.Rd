% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_fun.R
\name{pool_markers}
\alias{pool_markers}
\title{Pool markers}
\usage{
pool_markers(
  files,
  marker_ids = NULL,
  outfile = NULL,
  asv_with_centroids = NULL,
  method = "mean",
  vsearch_path = "vsearch",
  num_threads = 0,
  sep = ",",
  quiet = T,
  log_file = NULL
)
}
\arguments{
\item{files}{Character vector of file paths to the datasets to pool.
Each file must follow the same format, containing \code{asv_id}, \code{sample},
\code{read_count}, \code{asv}, and optionally \code{replicate}.}

\item{marker_ids}{Optional character or integer vector specifying marker IDs.
Must be positive non-zero integers and provided in the same order as
\code{files}. These IDs are appended to \code{asv_id} to ensure uniqueness across
markers. If not provided, \code{asv_id}s must already be unique across datasets.}

\item{outfile}{Character string specifying the output CSV file name.
If NULL, no file is written.}

\item{asv_with_centroids}{Character string specifying an optional output CSV
file containing the merged dataset annotated with \code{centroid_id} and
\code{centroid} columns.}

\item{method}{Character string specifying how read counts are aggregated
within groups. Must be one of \code{"mean"}, \code{"max"}, \code{"sum"}, or \code{"min"}.}

\item{vsearch_path}{Character string specifying the path to the \code{vsearch}
executable.}

\item{num_threads}{Positive integer specifying the number of CPUs to use.
If 0, all available CPUs are used.}

\item{sep}{Character string specifying the field separator used in input
and output CSV files.}

\item{quiet}{Logical; if TRUE, suppress informational messages and show only
warnings or errors.}

\item{log_file}{Character string specifying the path to the CSV log file.
If \code{NULL}, no log file is written.}
}
\value{
A data frame with columns \code{asv_id}, \code{sample}, \code{read_count},
\code{asv}, and optional \code{replicate}, where ASVs belonging to the same group
within each sample–replicate are merged into a single row. The \code{read_count}
reflects the selected aggregation method (mean, max, min, or sum).
}
\description{
Combine multiple datasets from the same samples generated using different
markers targeting overlapping genomic regions.
}
\details{
Input files must be in long format and contain the columns:
\code{asv_id}, \code{sample}, \code{read_count}, \code{asv}, and an optional \code{replicate}.

ASVs sharing identical sequences across overlapping regions are grouped
together. Within each group, a centroid sequence is defined as the ASV
with the highest total read count. Read counts are then aggregated across
ASVs within each group for each sample–replicate combination.

To avoid ambiguity between identical \code{asv_id}s originating from different
markers, optional marker identifiers can be appended to \code{asv_id}.
}
\examples{
\dontrun{
files <- c(
  "~/vtamR_demo_out_zfzr/filter/7_filter_chimera.csv",
  "~/vtamR_demo/filter/7_filter_chimera.csv"
)
marker_ids <- c(1, 2)
pool_markers(files, marker_ids = marker_ids, method = "mean")
}
}
