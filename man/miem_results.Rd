% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_helper_functions.R
\name{miem_results}
\alias{miem_results}
\title{Generate summary files for the MIEM sequencing summary statistics}
\usage{
miem_results(
  info_files = NULL,
  fastq_dir = ".",
  read_count_files = NULL,
  taxa = NULL,
  outdir = ".",
  mock_composition = NULL,
  sep = ","
)
}
\arguments{
\item{info_files}{Character vector of information files
(\code{fastqinfo}, \code{fastainfo}, or \code{sampleinfo}) containing information
such as FASTQ/FASTA file names, sample names, and sample types
(\code{real}, \code{mock}, or \code{negative}). These files are generated at
different preprocessing steps. In this function they are used to
calculate the total number of reads after the preprocessing steps of vtamR.
The last one is also used to determine sample information.}

\item{fastq_dir}{Character string specifying the directory containing
FASTQ files. Used to calculate read counts when they are not already
available in \code{info_files}.}

\item{read_count_files}{Character vector of read-count files generated
during the pipeline. Each file contains \code{asv_id}, \code{sample},
\code{read_count}, and optionally \code{replicate} and \code{cluster_id}.
These files are used to summarize the number of reads retained
during preprocessing and filtering.

A read-count file from any filtering step can be provided. The two
most informative files are usually the first file (typically the
output of \code{dereplicate}) and the last file, as they provide the
number of reads and ASVs after the preprocessing (merge, demultiplex and quality
filter) and after vtamR filtering (e.g. chimera, low, frequency noise), respectively.

The last file in the vector is also used to count the number of
ASVs or mOTUs assigned to each taxonomic rank and to identify
false-positive and false-negative assignments.

If the last file is the output of \code{cluster_asv} and contains a
\code{cluster_id} column, the number of mOTUs rather than ASVs is used
for the taxonomic summaries.}

\item{taxa}{Data frame containing taxonomic assignments. It must contain
\code{asv_id} (or \code{cluster_id}) as well as the taxonomic ranks
\code{domain}, \code{phylum}, \code{class}, \code{order},
\code{family}, \code{genus}, and \code{species} and may contain \code{ltg_rank_index}.
It is used to summarize the number
of ASVs or mOTUs assigned at each taxonomic rank.}

\item{outdir}{Character string specifying the directory where result
files will be written. The directory is created if necessary.
Defaults to the current working directory.}

\item{mock_composition}{Mock-community composition used to evaluate
control samples and identify false-positive and false-negative
assignments.}

\item{sep}{Character string used as the field separator for input
and output files. Defaults to \code{","}.}
}
\value{
Invisibly returns \code{NULL}. Results are written as output files
to \code{outdir}.
}
\description{
Generates summary statistics and quality-control metrics from the
intermediate and final outputs of the vtmaR pipeline. The resulting
files can be used to complete the "Results - Sequencing Summary
Statistics" section of the MIEM checklist.
}
\details{
Depending on the input files provided, the function summarizes
sequencing read counts, read counts per sample and sample type,
taxonomic assignments, and results from mock-community controls.
All generated results are written to files in \code{outdir}.

The function generates different result files depending on the
arguments supplied.

If \code{info_files} is provided, the total number of reads associated
with each information file is calculated using
\code{\link[=count_reads_from_info]{count_reads_from_info()}}. Existing read counts are used when
available; otherwise, reads are counted directly from the FASTQ files.
The results are written to \code{preprocess_read_count.csv}.

If both \code{info_files} and \code{read_count_files} are provided, read counts
are summarized for each read-count file and separately for real, mock,
and negative samples. The results are
written to \code{read_count_by_sample.csv}.

If both \code{taxa} and \code{read_count_files} are provided, the number of ASVs
or mOTUs assigned at each major taxonomic rank is calculated using
\code{\link[=count_taxassing_by_rank]{count_taxassing_by_rank()}} from the last file in \code{read_count_files}.
The results are written to
\code{ASV_or_mOTU_count_by_taxonomic_rank.csv}.

If \code{info_files}, \code{read_count_files} and \code{mock_composition} are
provided, mock-community control results are evaluated using
\code{\link[=classify_control_occurrences]{classify_control_occurrences()}} from the last read_count_files.
False-positive and false-negative
occurrences are combined and written to
\code{false_positives_and_negatives.csv}.
}
\examples{
\dontrun{
miem_results(
  info_files = info_files,
  fastq_dir = "fastq",
  read_count_files = read_count_files,
  taxa = taxa,
  outdir = "miem",
  mock_composition = mock_composition
)
}

}
