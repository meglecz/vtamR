% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_fun.R
\name{random_sample_batch}
\alias{random_sample_batch}
\title{Randomly Subsample a Batch of FASTA or Paired-End FASTQ Files}
\usage{
random_sample_batch(
  info,
  dir,
  outdir,
  n = NULL,
  p = NULL,
  N = NULL,
  chunk_lines = 1e+05,
  randseed = NULL,
  compress = FALSE,
  compress_method = "R",
  pigz_path = "pigz",
  num_threads = 0,
  fast_count = TRUE,
  sep = ",",
  quiet = TRUE,
  log_file = NULL
)
}
\arguments{
\item{info}{Character string with path to the input information file or data frame
describing the sequence files. The file must contain either a \code{fasta} column or both
\code{fastq_fw} and \code{fastq_rv} columns. An optional \code{read_count} column may
contain the number of reads in each file.}

\item{dir}{Character string. Directory containing the input sequence files
listed in \code{info}.}

\item{outdir}{Character string. Directory where the subsampled sequence
files and updated information file will be written.}

\item{n}{Numeric or integer. Number of reads to randomly sample from each
input file. If a file contains fewer than \code{n} reads, all reads are retained.
Mutually exclusive with \code{p} and \code{N}.}

\item{p}{Numeric. Proportion of reads to randomly sample from each input
file. Must typically be between 0 and 1. The number of sampled reads is
calculated by rounding \code{p * read_count}. Mutually exclusive with \code{n} and
\code{N}.}

\item{N}{Integer. Total number of reads to sample across all
input files. Reads are allocated proportionally according to the number of
reads in each file. If \code{N} is greater than or equal to the total number of
reads, no proportional reduction is applied. Mutually exclusive with \code{n}
and \code{p}.}

\item{chunk_lines}{Numeric. Number of lines to process at a time when
counting or sampling sequences. Larger values may improve performance but
require more memory. Defaults to \code{1e5}.}

\item{randseed}{Numeric or integer, or \code{NULL}. Random seed used to make
sampling reproducible. If \code{NULL}, no seed is explicitly set.}

\item{compress}{Logical. If \code{TRUE}, output sequence files are compressed and
output filenames are generated accordingly. Defaults to \code{FALSE}.}

\item{compress_method}{Character string. Must be one of \code{"pigz"}, \code{"gzip"}, or \code{"R"}.
Compression method used when writing output files.
Defaults to \code{"R"}. Only relevant when a file has
fewer sequences than required and is copied to the output directory with
a different compression state than the input.}

\item{pigz_path}{Character string: path to the \code{pigz} executable. Only required if
\code{pigz} is used and not available in the system PATH.
Only relevant when a file has
fewer sequences than required and is copied to the output directory with
a different compression state than the input.}

\item{num_threads}{Positive integer: number of CPU threads to use.
If 0, all available CPUs are used. Only relevant when a file has
fewer sequences than required and is copied to the output directory with
a different compression state than the input.}

\item{fast_count}{Logical. If \code{TRUE}, uses the faster read-counting method
implemented by \code{\link[=count_reads]{count_reads()}}. Defaults to \code{TRUE}. Available only in linux-like
sytems.}

\item{sep}{Character string. Field separator used to read and write the
information file. Defaults to \code{","}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress and informational
messages where supported. Defaults to \code{TRUE}.}

\item{log_file}{Character string specifying the path to the CSV log file.
The path is resolved with the following priority:
\enumerate{
\item the \code{log_file} argument, if explicitly provided by the user;
\item the package-level option/variable storing a default log path
(if set);
\item \code{"vtamR_log.csv"} in the current working directory, used as a
last resort if neither of the above is set.
}
To disable logging entirely, set \code{log_file = NA}.}
}
\value{
A data frame containing the updated information table. Input sequence
file columns are replaced with the names of the subsampled output
files, and a \code{read_count} column is added containing the number of reads
retained after sampling. The updated information table is also written to
\code{outdir} using the basename of \code{info}.
}
\description{
Randomly subsamples sequences from multiple FASTA files or paired-end FASTQ
files described in an input information file. The sampling size can be
specified as a fixed number of reads per file (\code{n}), a proportion of reads
per file (\code{p}), or a total number of reads across all files (\code{N}).
}
\details{
Exactly one of \code{n}, \code{p}, or \code{N} must be specified. When read counts are not
available in the input information file, they are calculated automatically.
The function then performs random subsampling on each input file and writes
the resulting files and an updated information file to \code{outdir}.

For FASTA input, the information file must contain a \code{fasta} column. For
paired-end FASTQ input, it must contain both \code{fastq_fw} and \code{fastq_rv}
columns. An optional \code{read_count} column can be supplied to avoid recounting
reads in the input files.

The function first determines whether the input describes FASTA files or
paired-end FASTQ files. If read counts are missing or incomplete, all input
files are counted using \code{\link[=count_reads]{count_reads()}}.

Sampling is then performed independently for each file:
\itemize{
\item With \code{n}, up to \code{n} reads are sampled from each file.
\item With \code{p}, the same proportion of reads is sampled from each file.
\item With \code{N}, reads are allocated proportionally across all files based on
their original read counts.
}

FASTA files are processed with \code{\link[=random_sample_fasta]{random_sample_fasta()}}, while paired-end
FASTQ files are processed with \code{\link[=random_sample_fastq]{random_sample_fastq()}}. For paired-end FASTQ
data, forward and reverse reads are sampled together to preserve pairing.
}
\examples{
\dontrun{
# Sample up to 1000 reads from each file
random_sample_batch(
  info = "samples.csv",
  dir = "input_sequences",
  outdir = "subsampled_sequences",
  n = 1000
)

# Sample 10\% of reads from each file
random_sample_batch(
  info = "samples.csv",
  dir = "input_sequences",
  outdir = "subsampled_sequences",
  p = 0.1,
  randseed = 123
)

# Sample approximately 1 million reads distributed proportionally
# across all input files
random_sample_batch(
  info = "samples.csv",
  dir = "input_sequences",
  outdir = "subsampled_sequences",
  N = 1e6
)
}

}
\seealso{
\code{\link[=random_sample_fasta]{random_sample_fasta()}}, \code{\link[=random_sample_fastq]{random_sample_fastq()}}, \code{\link[=count_reads]{count_reads()}},
\code{\link[=fast_copy]{fast_copy()}}, \code{\link[=smart_gzip]{smart_gzip()}}.
}
