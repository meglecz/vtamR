% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_helper_functions.R
\name{count_reads_from_info}
\alias{count_reads_from_info}
\title{Count reads from sample information files}
\usage{
count_reads_from_info(
  info_files = NULL,
  fastq_dir = ".",
  outdir = ".",
  sep = ","
)
}
\arguments{
\item{info_files}{Character vector of sample information files.}

\item{fastq_dir}{Character string specifying the directory containing
FASTQ files. Defaults to the current working directory.}

\item{outdir}{Character string specifying the output directory.
Defaults to the current working directory.}

\item{sep}{Character string used as the field separator when reading
and writing files. Defaults to \code{","}.}
}
\value{
A data frame with one row per information file and two columns:
\code{info_filename}, containing the input file name, and \code{read_count},
containing the total number of reads. The same data frame is written
to \code{preprocess_read_count.csv} in \code{outdir}.
}
\description{
Calculates the total number of reads associated with each sample
information file. If an input file already contains a \code{read_count}
column, the existing counts are summed. Otherwise, reads are counted
directly from the FASTQ files listed in the \code{fastq_fw} column.
}
\details{
The resulting read counts are written to
\code{preprocess_read_count.csv} in \code{outdir}.

For information files containing a \code{read_count} column, the function
retains the last two columns, removes duplicate rows, and sums the
read counts.

For information files without a \code{read_count} column, the function
extracts unique FASTQ file names from the \code{fastq_fw} column and counts
the reads in each FASTQ file using \code{\link[=count_reads]{count_reads()}}.
}
\keyword{internal}
