% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_fun.R
\name{match_variants_to_mock_species}
\alias{match_variants_to_mock_species}
\title{Match ASV variants to expected mock species}
\usage{
match_variants_to_mock_species(
  read_count,
  fas,
  sampleinfo,
  taxonomy,
  outdir,
  blast_path = "blastn",
  num_threads = 0,
  sep = ",",
  quiet = TRUE,
  log_file = NULL
)
}
\arguments{
\item{read_count}{Data frame or CSV file containing columns \code{asv_id}, \code{sample}, \code{asv}, and \code{read_count}.}

\item{fas}{Character string: path to a FASTA file containing reference sequences from expected mock species
or closely related taxa. FASTA headers must follow the format:
\verb{>HQ563207.1 taxID=1592914}, where \code{taxID} is a valid NCBI taxonomic identifier
(https://www.ncbi.nlm.nih.gov/taxonomy).}

\item{sampleinfo}{Data frame or CSV file containing at least \code{sample} and \code{sample_type} columns.}

\item{taxonomy}{Character string: path to a TSV taxonomy file containing columns:
\code{tax_id}, \code{parent_tax_id}, \code{rank}, \code{name_txt}, \code{old_tax_id} (merged into \code{tax_id}),
and \code{taxlevel} (8: species, 7: genus, 6: family, 5: order, 4: class, 3: phylum,
2: kingdom, 1: domain, 0: root).
A COInr taxonomy file can be used.}

\item{outdir}{Character string: output directory for generated files.}

\item{blast_path}{Character string: path to BLAST executable.}

\item{num_threads}{Positive integer: number of CPU threads to use. If 0, all available CPUs are used.}

\item{sep}{Field separator character used in input and output CSV files.}

\item{quiet}{Logical: if TRUE, suppress informational messages and show only warnings or errors.}

\item{log_file}{Character string specifying the path to the CSV log file.
The path is resolved with the following priority:
\enumerate{
\item the \code{log_file} argument, if explicitly provided by the user;
\item the package-level option/variable storing a default log path
(if set);
\item \code{"vtamR_log.csv"} in the current working directory, used as a
last resort if neither of the above is set.
}
To disable logging entirely, set \code{log_file = NA}}
}
\value{
Data frame with columns: \code{sample}, \code{action}, \code{asv}, \code{taxon}, \code{asv_id}.
}
\description{
Identifies expected variants in mock samples by matching ASVs to a
custom reference database.
}
\details{
This function performs the following steps:
\itemize{
\item Builds a small BLAST database from sequences of species expected in the mock samples
(or closely related taxa). Reference sequences should at least
partially cover the target amplicon
(approximately 70\% coverage) and may be shorter or longer than the ASVs.
\item Assigns taxonomy to all ASVs detected in mock samples using this custom database.
\item Selects the most abundant ASV for each taxon.
\item Generates a \code{mock_composition} template file for user validation and manual curation.
}

The output directory contains two files:
\itemize{
\item \emph{mock_taxassign.csv}:
Taxonomic assignment results, including total read counts per ASV in mock samples
and the number of mock samples in which each ASV is detected.
\item \emph{mock_composition_template_to_check.csv}:
A template for constructing the \code{mock_composition} file.
It includes the most abundant sequence for each \code{ltg_name} identified in the taxonomic assignment,
repeated across mock samples.

When multiple mock samples have different expected compositions, users should remove lines
corresponding to taxa not expected in each sample.

Note: this file does not include reference species that did not match any ASV.
This may occur if the species was not amplified in the mock samples or if the reference
sequence in the custom database is incorrect.
}
}
\examples{
\dontrun{
match_variants_to_mock_species(
  read_count = read_count_df,
  fas = "reference.fasta",
  taxonomy = "taxonomy.tsv",
  blast_path = blast_path,
  sampleinfo = sampleinfo_df
)
}
}
