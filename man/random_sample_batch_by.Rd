% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vtamR_fun.R
\name{random_sample_batch_by}
\alias{random_sample_batch_by}
\title{Randomly Subsample Sequence Files by Group}
\usage{
random_sample_batch_by(
  info,
  dir,
  outdir,
  by = "replicate",
  N = NULL,
  chunk_lines = 1e+05,
  randseed = NULL,
  compress = FALSE,
  compress_method = "R",
  pigz_path = NULL,
  num_threads = 0,
  fast_count = TRUE,
  sep = ",",
  quiet = TRUE,
  log_file = NULL
)
}
\arguments{
\item{info}{Character string with path to the input information file or
data frame describing the sequence files. The file must contain either a
\code{fasta} column or both \code{fastq_fw} and \code{fastq_rv} columns. An optional
\code{read_count} column may contain the number of reads in each file.}

\item{dir}{Character string. Directory containing the input sequence files
listed in \code{info}.}

\item{outdir}{Character string. Directory where the subsampled sequence
files and updated information file will be written.}

\item{by}{Character string specifying the name of the column used to split
the input information table into groups. \code{\link[=random_sample_batch]{random_sample_batch()}} is called
once for each distinct value in this column. Defaults to \code{"replicate"}.}

\item{N}{Integer. Total number of reads to sample within each
group. Reads are allocated proportionally according to the number of reads
in each file within that group. If \code{N} is greater than or equal to the
total number of reads in a group, input files are copied to the output directory
without subsampling.}

\item{chunk_lines}{Numeric. Number of lines to process at a time when
counting or sampling sequences. Larger values may improve performance but
require more memory. Defaults to \code{1e5}.}

\item{randseed}{Integer, or \code{NULL}. Random seed used to make
sampling reproducible. If \code{NULL}, no seed is explicitly set.}

\item{compress}{Logical. If \code{TRUE}, output sequence files are compressed and
output filenames are generated accordingly. Defaults to \code{FALSE}.}

\item{compress_method}{Character string. Must be one of \code{"pigz"}, \code{"gzip"},
or \code{"R"}. Compression method used when writing output files.
Defaults to \code{"R"}. Only relevant when a files have fewer sequences than
required and is copied to the output directory with a different
compression state than the input.}

\item{pigz_path}{Optional path to the \code{pigz} executable. If
\code{NULL} (default), resolved via \code{getOption("vtamR.pigz_path")}
or the \code{PATH} (see \code{\link{get_path}}).}

\item{num_threads}{Positive integer: number of CPU threads to use.
If 0, all available CPUs are used. Only relevant when a file has fewer
sequences than required and is copied to the output directory with a
different compression state than the input.}

\item{fast_count}{Logical. If \code{TRUE}, uses the faster read-counting method
implemented by \code{\link[=count_reads]{count_reads()}}. Defaults to \code{TRUE}. Available only on
Linux-like systems.}

\item{sep}{Character string. Field separator used to read and write the
information file. Defaults to \code{","}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress and informational
messages where supported. Defaults to \code{TRUE}.}

\item{log_file}{Character string specifying the path to the CSV log file.
The path is resolved with the following priority:
\enumerate{
\item the \code{log_file} argument, if explicitly provided by the user;
\item the \code{vtamR.log_file} option set with
\code{options(vtamR.log_file = ...)}, if set;
\item \code{"vtamR_log.csv"} in the current working directory, used as a
last resort if neither of the above is set.
}
To disable logging entirely, set \code{log_file = NA}.}
}
\value{
A data frame containing the combined updated information tables
returned by \code{\link[=random_sample_batch]{random_sample_batch()}} for each group. Input sequence file
columns are replaced with the names of the subsampled output files, and
a \code{read_count} column is added containing the number of reads retained
after sampling. The combined information table is also written to
\code{outdir}.
}
\description{
Randomly subsamples sequences from multiple FASTA files or paired-end FASTQ
files described in an input information file, independently for each
distinct value of a specified column.
}
\details{
This is a convenience wrapper around \code{\link[=random_sample_batch]{random_sample_batch()}}. The input
information table is split according to the column specified by \code{by}, and
\code{\link[=random_sample_batch]{random_sample_batch()}} is called separately for each resulting group.
The updated information tables are then combined and written to a single
output information file.

The sampling size can be specified as a total number of reads per group
(\code{N}). Reads are allocated proportionally according to the number of reads
in each file within each group. Consequently, the total number of reads
sampled across all groups may exceed \code{N}.

For FASTA input, the information file must contain a \code{fasta} column. For
paired-end FASTQ input, it must contain both \code{fastq_fw} and \code{fastq_rv}
columns. An optional \code{read_count} column can be supplied to avoid recounting
reads in the input files.

The function first reads the input information table and extracts the
distinct values of the column specified by \code{by}. For each value, it
subsets the information table and calls \code{\link[=random_sample_batch]{random_sample_batch()}} with
\code{N = N}. The resulting updated information tables are combined.

Sampling is therefore performed independently within each group:
\itemize{
\item With \code{N}, up to \code{N} reads are sampled across the files belonging to each
group, allocated proportionally according to their original read counts.
\item The same input sequence file may be processed separately in different
groups if it appears in more than one group.
}

The function determines whether the input describes FASTA files or
paired-end FASTQ files and writes the combined updated information table
to \code{fastainfo.csv} or \code{fastqinfo.csv}, respectively.
}
\examples{
\dontrun{
# Sample approximately 1 million reads independently within each replicate
random_sample_batch_by(
  info = "samples.csv",
  by = "replicate",
  dir = "input_sequences",
  outdir = "subsampled_sequences",
  N = 1e6
)

# Sample 5000 reads independently within each condition
random_sample_batch_by(
  info = "samples.csv",
  by = "condition",
  dir = "input_sequences",
  outdir = "subsampled_sequences",
  N = 5000,
  randseed = 123
)
}

#' @export
}
\seealso{
\code{\link[=random_sample_batch]{random_sample_batch()}}, \code{\link[=random_sample_fasta]{random_sample_fasta()}}, \code{\link[=random_sample_fastq]{random_sample_fastq()}},
\code{\link[=count_reads]{count_reads()}}, \code{\link[=fast_copy]{fast_copy()}}, \code{\link[=smart_gzip]{smart_gzip()}}.
}
