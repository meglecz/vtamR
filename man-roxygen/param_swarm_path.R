#' @param swarm_path Optional path to the \code{swarm} executable. If
#'   \code{NULL} (default), resolved via \code{getOption("vtamR.swarm_path")}
#'   or the \code{PATH} (see \code{\link{get_path}}).
