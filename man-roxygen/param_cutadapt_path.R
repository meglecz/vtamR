#' @param cutadapt_path Optional path to the \code{cutadapt} executable. If
#'   \code{NULL} (default), resolved via \code{getOption("vtamR.cutadapt_path")}
#'   or the \code{PATH} (see \code{\link{get_path}}).
