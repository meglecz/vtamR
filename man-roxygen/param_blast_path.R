#' @param blast_path Optional path to the \code{blast} executable. If
#'   \code{NULL} (default), resolved via \code{getOption("vtamR.blast_path")}
#'   or the \code{PATH} (see \code{\link{get_path}}).
