#' @param vsearch_path Optional path to the \code{vsearch} executable. If
#'   \code{NULL} (default), resolved via \code{getOption("vtamR.vsearch_path")}
#'   or the \code{PATH} (see \code{\link{get_path}}).
